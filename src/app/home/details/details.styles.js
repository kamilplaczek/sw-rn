import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
    alignItems: 'center',
    padding: 20,
  },
  openingText: {
    fontSize: 10,
    color: 'yellow',
    position: 'absolute',
    padding: 50,
  },
});
